# MOD作成・更新チェックリスト

新規MODの作成時と、公開版を更新する直前に確認します。該当しない項目は `N/A` と記録します。

基準となるMOMIの最新公開版：**v0.16.4**（2026-09-22確認）。
この版より新しいMOMIまたはゲーム本編の更新があった場合は、公開・更新前に
リリースノートと動作を再確認します。

動作確認結果と不具合は、公開の有無にかかわらず
[COMPATIBILITY_AND_BUG_LOG.md](COMPATIBILITY_AND_BUG_LOG.md) に追記します。
ゲーム版がゲーム内で確認できない場合は、推測せず `未記録` と書きます。

## 更新時の確認記録

- [ ] ゲーム本編またはMOMIの更新内容を読み、影響しそうなMOD・機能を記録した。
- [ ] 使用したゲーム版、MOMI版、MOD版、確認日、結果を互換性・不具合記録へ記入した。
- [ ] エラー、違和感、未確認項目があれば、再現手順と暫定対応を不具合記録へ記入した。
- [ ] 問題がなければ、確認した主要機能を簡潔に記録した。

## 1. 範囲と構成

- [ ] 動作確認に使用した MOMI の公開バージョンを記録した：`v____`。
- [ ] `manifest.toml` の `minInstallerVersion` が、動作確認した MOMI 版以下であることを確認した：`v____`。
- [ ] ゲームフォルダ内のアセットを直接編集していない。
- [ ] MODフォルダには、そのMODに必要な自作ファイルだけが入っている。
- [ ] `manifest.toml` の `name`、`author`、`version`、`minInstallerVersion` を確認した。
- [ ] `mmapi_on`、`mmapi_filter`、`mmapi_guard`、`mmapi_override` を使う場合、対応する正式なフック名を `requires_hooks` にすべて記載した。
- [ ] MOD ID・フォルダ名を、公開済みのMODから不用意に変更していない。
- [ ] READMEに機能、前提MOMI版、導入方法、設定方法、既知の制限を記載した。
- [ ] CHANGELOGに今回の変更とバージョンを記載した。

## 2. MOMI / MMAPI実装

- [ ] フック・登録処理は一度だけ実行される（重複登録しない）。
- [ ] 毎フレームの処理は必要な対象・範囲だけに限定した。
- [ ] 動的なゲーム内部値は、`undefined` や未解放状態を確認してから使う。
- [ ] UIは可変長の地域名・文章に依存せず、固定アンカーで配置した。
- [ ] ホットキーを使う場合、既存MODとの衝突を避けるか、変更手段を用意した。
- [ ] エラー確認用のログまたは通知は、通常プレイを妨げない状態にした。
- [ ] プレイヤーへ表示する固定文言は、GMLの直書きではなく、Fiddle文字列と `localization/l10n.meta.toml` の翻訳キーを使用した。動的な本編名称は本編の翻訳キーから取得した。

## 3. 設定

- [ ] 各設定に分かりやすい表示名、初期値、用途を用意した。
- [ ] Boolean・数値・文字列を型検証した。
- [ ] 通常の設定読込には `mmapi_config_read_valid` を使用した。旧設定を移行するために低レベル読込を使う場合は、移行対象の版と理由をコメントに記録した。
- [ ] 数値の最小値・最大値を設定した。範囲外の値は仕様どおり初期値へ戻ることを確認した。
- [ ] 設定の形式を互換性なく変えた場合だけ、設定バージョンを上げた。
- [ ] 旧設定を安全に初期値へ移行できることを確認した。
- [ ] 機能を停止できる `enabled` 設定が必要な場合、OFF時には処理が実行されない。

## 4. 動作確認

- [ ] MOMI CLIを使用できる環境では、コンパイル確認を実行した。実行できない場合は、その理由と代替の静的確認を記録した。
- [ ] MODを入れた状態で、ゲームがタイトル画面からセーブデータまで起動した。
- [ ] MODのUI・設定を開けた。
- [ ] 主機能をゲーム内で実行し、期待どおりの結果を確認した。
- [ ] エラー時や対象外の状態で、ゲームが終了・フリーズしないことを確認した。
- [ ] 他の自作MODを有効にした状態でも確認した。
- [ ] 同じMODの旧版が同時に有効になっていないことを確認した。

## 5. 配布前

- [ ] ZIPの最上位がMODフォルダになっている。
- [ ] ZIPにゲーム本体、`mod_data`、ログ、作業用ファイル、過去版ZIPが入っていない。
- [ ] ZIPを展開して、`manifest.toml` と必要なGML/Fiddleファイルが揃っていることを確認した。
- [ ] Nexus Modsの説明・スクリーンショット・依存関係・対応バージョンを更新した。
- [ ] Steamの整合性チェック後は、必要に応じてMOMIを再インストールする案内を記載した。
